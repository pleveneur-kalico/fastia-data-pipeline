# Module 2 — Préparation des données structurées
# Environnement Python recommandé : 3.11

# Manipulation et analyse de données
pandas>=2.2
numpy>=1.26
pyarrow>=15.0

# Visualisation
matplotlib>=3.8
seaborn>=0.13
missingno>=0.5

# Notebook
jupyter>=1.0
ipykernel>=6.29

# NLP — détection d'entités, normalisation
spacy>=3.7
# Modèle français à installer en plus :
#   python -m spacy download fr_core_news_sm

# Augmentation de données
nltk>=3.8

# Validation / qualité
ydata-profiling>=4.8   # rapport HTML de profiling (bonus B2)

# Stockage relationnel (Brief 3)
sqlalchemy>=2.0
psycopg[binary]>=3.1   # PostgreSQL — retirer si MySQL choisi
# pymysql>=1.1         # décommenter si MySQL choisi
alembic>=1.13          # versionnement de schéma (bonus B3)

# Qualité de code
loguru>=0.7
pytest>=8.0
pytest-cov>=5.0

# Appel LLM pour augmentation synthétique (Brief 3)
httpx>=0.27
python-dotenv>=1.0
